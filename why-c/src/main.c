#include "why.h"
#include <stdio.h>
#include <stdlib.h>


int main(int argc, char **argv) {
    if (argc < 2) {
        printf("Missing required argument: the file to compile.");
        exit(1);
    }



    return 0;
}
